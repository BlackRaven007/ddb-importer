<section id="ddb-importer-muncher-tools" class="tab ddb-muncher {{ tab.cssClass }} {{#if tab.active}} active{{/if}}" data-tab="{{ tab.id }}" data-group="{{ tab.group }}">
    <fieldset>
    <legend>Magic Item Prices</legend>
      <p>Set magic item prices to values as per <a href="https://www.dndbeyond.com/sources/xgte/downtime-revisited#MagicItemPrice">Xanathar's Guide</a> rules. This will set items which have a rarity specified and no/0 cost to a random price in the range.</p>
      <div>
        <button data-action="setPricesXanathar" type="button" id="munch-xanathar-price" class="munch-button"><i class="fas fa-coins"></i> Set Magic Item Prices</button>
      </div>
    </fieldset>
    <fieldset>
    <legend>Monsters</legend>
      <p>Reset compendium actor images back to munched defaults.</p>
      <div>
        <button data-action="resetCompendiumActorImages" type="button" id="munch-reset-images" class="munch-button"><i class="fas fa-person-booth"></i> Reset compendium actor images</button>
      </div>
      <p>Update world actors with compendium actors (only targets DDB imported monsters).</p>
      {{#each worldUpdateConfig}}
      <div class="form-group munching-monster-world-update-config">
        <label>{{ this.label }}</label>
        <div class="form-fields">
          <dnd5e-checkbox name="{{this.name}}" id="{{this.name}}" {{#if this.isChecked}}checked{{/if}}
          data-section="{{this.name}}" {{#if this.enabled}}enabled{{else}}disabled{{/if}}></dnd5e-checkbox>
        </div>
        <p class="hint">{{{this.hint}}}</p>
      </div>
      {{/each}}
      <div>
        <button data-action="updateWorldActors" type="button" id="munch-world-monster-update" class="munch-button"><i class="fas fa-user-edit"></i> Update world monsters</button>
      </div>
      <div>
        <button data-action="selectiveUpdateWorldActors" type="button" id="munch-world-monster-selective-update" class="munch-button"><i class="fas fa-filter"></i> Selective update world monsters</button>
      </div>
    </fieldset>
    <fieldset>
    <legend>Token Frames</legend>
      <div>
        <p>Munch token frames from D&D Beyond.</p>
        <button data-action="parseFrames" type="button" id="munch-frames-start" class="munch-button"
          {{#if tiers.supporter}}enabled{{else}}disabled{{/if}}><i class="fas fa-universal-access"></i>{{#if tiers.supporter}}Token Frame Munch{{else}}Token Frames for Patreon Supporters{{/if}}</button>
      </div>
    </fieldset>
    <fieldset>
    <legend>Regenerate Storage</legend>
      <div>
        <p>DDB Importer stores things like the compendium banner images in local module storage. If this gets deleted you can redownload the needed files here.</p>
        <button data-action="regenerateStorage" type="button" id="munch-regenerate-storage" class="munch-button"
          <i class="fas fa-universal-access"></i>Regenerate Storage</button>
      </div>
    </fieldset>
    <fieldset>
    <legend>Import Recovery</legend>
      <p>Resume the latest partial-failed import run without reprocessing successful entries.</p>
      <div>
        <button data-action="resumeLastImport" type="button" id="munch-import-resume-last" class="munch-button"><i class="fas fa-redo"></i> Resume Last Import</button>
      </div>
      <p>Review recent import runs and failed entries captured for resume-after-failure.</p>
      <div>
        <button data-action="showImportRecoveryReport" type="button" id="munch-import-recovery-report" class="munch-button"><i class="fas fa-list"></i> Show Import Recovery Report</button>
      </div>
      <p>Clear all stored import checkpoints and failure history if you want a clean import baseline.</p>
      <div>
        <button data-action="clearImportRecoveryState" type="button" id="munch-import-recovery-clear" class="munch-button"><i class="fas fa-trash"></i> Clear Import Recovery State</button>
      </div>
    </fieldset>
</section>
